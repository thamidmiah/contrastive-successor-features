# ============================================================
# Docker-specific Python requirements
# Exact versions from working conda 'csf' environment
# Platform: Linux x86_64 with CUDA 12.1
# Python: 3.9.25
# ============================================================
# NOTE: torch/torchvision are installed separately in the
# Dockerfile with --index-url for CUDA support.

# ── Core numeric / scientific ──
numpy==1.26.4
scipy==1.13.1
scikit-learn==1.6.1
scikit-image==0.24.0
pandas==2.3.3
h5py==3.14.0
sympy==1.14.0

# ── RL framework ──
akro==0.0.8
gym==0.26.2
gymnasium==1.1.1
ale-py==0.11.2

# ── Image / video ──
opencv-python-headless==4.13.0.92
pillow==11.3.0
imageio==2.37.2
imageio-ffmpeg==0.6.0
moviepy==2.2.1
matplotlib==3.9.4

# ── Logging & experiment tracking ──
tensorboard==2.16.2
tensorboardX==2.6.4
wandb==0.25.0

# ── TensorFlow (Linux build, for TensorBoard + tfp) ──
tensorflow==2.16.2
tensorflow-probability==0.25.0

# ── Optimization ──
cma==4.4.2

# ── Utilities ──
click==8.1.8
cloudpickle==3.1.2
tqdm==4.67.3
pyyaml==6.0.3
psutil==7.2.2
python-dateutil==2.9.0.post0
requests==2.32.5
tabulate==0.9.0
setproctitle==1.3.7
better-exceptions==0.3.3
rich==14.3.2
GitPython==3.1.46
joblib==1.5.3
dm-tree==0.1.8
patchelf==0.17.2.4

# ── Core / transitive deps (pinned for reproducibility) ──
protobuf==4.25.8
grpcio==1.78.0
packaging==26.0
typing_extensions==4.15.0
filelock==3.19.1
fsspec==2025.10.0
networkx==3.2.1
